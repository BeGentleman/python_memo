0x00 什么是字符串解包
     当给了一个字符串 "abcdef"的时候，这是一个整体，现在想要把字符串都拆开成为一个一个的小字符串、
     这个过程就叫做解包。

0x01 字符串解包的函数
     用一个 * 就可以给字符串解包了。
     
     举例子：
            >>> str1 = "abcdef"
            >>> print(*str1)
            a b c d e f
            
