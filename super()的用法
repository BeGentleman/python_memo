0x01 super()的用法
     super用来表示是继承关系
     写法有两种：
     第一种：super(当前类名, self).函数名()
     第二种：super().函数名()
     这两种写法实现的效果是一样的，只是一个有参数，一个没参数。
     
0x02 super()的应用场景
     第一种：在一个类中，super().__init__()
            为了让子类可以调用父类的属性
            举例子：
            
